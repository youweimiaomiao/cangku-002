运用vite时，开启vite本地电脑服务器需要在html文件的层级下，用命令行手动进入路径运行vite，不然的话就会遇到服务器起来了但是HTML文件跑不进去的情况
vite build 可以构建架构，也就是反解耦，或者说打包碎片成一块
解释：由于包导入容易引发全局污染，也就是大家都用同一个变量名，所以引入了import和export进行模块化处理，然而模块化处理在html引入js文件时改用<type = "module">这种写法，也就是说ES写法完成，但是规则化的ES在15年被浏览器引入并认定为不安全，也就是要求必须在服务器上运行，不能本地直接打开使用，所以本地打开时会发现js文件引入失败，解决方法就是利用vite单独开一个本地服务器，而vite本身是用js打造的，为了能在浏览器环境外运行js文件，就下载了nodejs，vite解决了三个问题：1.通过创建本地服务器达成浏览器规则而解决ES模块化的js文件无法导入html的问题。2.拥有“热更新”，当本地文件保存时，浏览器实时输出最新内容，避开浏览器缓存机制的原理是通过文件本体计算出一个哈希值，文件改动会改动哈希值，哈希值被写在文件名上，文件名更改让浏览器强制更新，跳过缓存。3.vite build反解耦的打包碎片文件机制让浏览器的请求时间下降，用户体验更好，并且同步开启了热更新，让打包后的文件也有热更新机制。
有了"scripts": {
    "dev":"vite",
    "build":"vite build",
    "preview":"vite preview"
  },
  我们就可以直接
  npm run dev
  npm run build
  npm run preview

  animejs 这种光秃秃的写法浏览器是不认的，但是vite会帮我们把它翻译成node_modules下面的文件，就是指向对应的库，也就是翻译成浏览器能看懂的。